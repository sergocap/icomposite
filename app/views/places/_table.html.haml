.places
  %table
    - (0...places.maximum(:y)).each do |y|
      %tr
        - (0...places.maximum(:x)).each do |x|
          %td
            .place
            = image_tag places.where(:x => x, :y => y).first.original_image.url
